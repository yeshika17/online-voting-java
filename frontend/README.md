# Frontend (Minimal React-like)

This is a minimal frontend using plain JS modules (works with parcel or a simple static server).

## Dev (recommended)
Install parcel (no global install needed):
npx parcel src/index.html --port 3000

Then open http://localhost:3000

## Build
npx parcel build src/index.html

Note: frontend expects backend at http://localhost:8080
